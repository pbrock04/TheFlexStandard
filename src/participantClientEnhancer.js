const PARTICIPANT_KEY = 'flexStandard.participant.v1';

function injectBefore(source, needle, content) {
  return source.includes(needle) ? source.replace(needle, content + needle) : source;
}

export function enhanceFoundationParticipantFlow(source) {
  const gate = `<section id="participantGate" class="participant-gate" hidden><div class="participant-gate-card"><div class="participant-kicker">FREE 7-DAY FOUNDATION</div><h2>Start Your Foundation</h2><p>Enter your email so your progress can follow you as you build through The Flex Standard.</p><form id="participantStartForm" novalidate><label>Name <span>(optional)</span><input id="participantName" type="text" maxlength="100" autocomplete="name" placeholder="Your name"></label><label>Email<input id="participantEmail" type="email" maxlength="254" autocomplete="email" required placeholder="you@example.com"></label><button id="participantStartBtn" type="submit">START THE 7-DAY FOUNDATION</button><div id="participantStartStatus" class="participant-status" aria-live="polite"></div></form><small>Your email is used to support your challenge progress and program communication.</small></div></section>`;

  const css = `.participant-gate{position:fixed;inset:0;z-index:1000;display:grid;place-items:center;padding:20px;background:rgba(3,3,3,.94);backdrop-filter:blur(12px)}.participant-gate[hidden]{display:none!important}.participant-gate-card{width:min(560px,100%);background:#141414;border:1px solid rgba(212,175,55,.42);border-radius:22px;padding:clamp(22px,5vw,34px);box-shadow:0 24px 80px rgba(0,0,0,.45)}.participant-kicker{color:var(--gold);font-size:.72rem;font-weight:900;letter-spacing:.14em}.participant-gate-card h2{font-size:clamp(1.8rem,6vw,2.6rem);margin:.35rem 0}.participant-gate-card p,.participant-gate-card small{color:var(--muted)}.participant-gate-card form{display:grid;gap:.85rem;margin-top:1.2rem}.participant-gate-card label{font-size:.8rem;font-weight:800}.participant-gate-card label span{color:#777}.participant-gate-card input{width:100%;margin-top:.35rem;background:#0b0b0b;border:1px solid var(--border);border-radius:12px;color:var(--text);padding:.9rem;font:inherit}.participant-gate-card button{border:0;border-radius:999px;background:var(--gold);color:#080808;padding:.9rem 1rem;font:inherit;font-weight:900;cursor:pointer}.participant-status{min-height:1.4em;color:var(--muted);font-size:.84rem}.participant-status.error{color:#ef9a9a}`;

  const js = `\nconst FLEX_PARTICIPANT_KEY='${PARTICIPANT_KEY}';function readFlexParticipant(){try{return JSON.parse(localStorage.getItem(FLEX_PARTICIPANT_KEY)||'null')}catch(e){return null}}function readFoundationLocal(){try{const v=JSON.parse(localStorage.getItem('flexStandard.challenge7.v1')||'{}');const c=Array.isArray(v.completed)?[...new Set(v.completed.filter(n=>Number.isInteger(n)&&n>=1&&n<=7))].sort((a,b)=>a-b):[];return{completed:c}}catch(e){return{completed:[]}}}async function postFoundationProgress(userId,day){if(!userId||!day)return null;const r=await fetch('/api/participants/progress',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({user_id:userId,tier_id:'7_day_foundation',current_day:day})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Unable to save progress.');return d.progress}async function reconcileFoundation(participant){if(!participant||!participant.user_id)return;try{const r=await fetch('/api/participants/state?user_id='+encodeURIComponent(participant.user_id),{headers:{'cache-control':'no-cache'}});const d=await r.json();if(!r.ok||!d.ok)return;const tier=(d.state.tiers||[]).find(t=>t.tier_id==='7_day_foundation');const serverDay=Math.max(0,Number(tier&&tier.current_day||0));const local=readFoundationLocal();const localDay=local.completed.length;if(serverDay>localDay){local.completed=Array.from({length:Math.min(7,serverDay)},(_,i)=>i+1);localStorage.setItem('flexStandard.challenge7.v1',JSON.stringify(local));if(typeof renderChallenge==='function')renderChallenge()}else if(localDay>serverDay){await postFoundationProgress(participant.user_id,localDay)}}catch(e){console.warn('foundation_progress_reconcile_failed')}}const participantGate=document.getElementById('participantGate'),participantForm=document.getElementById('participantStartForm');let flexParticipant=readFlexParticipant();if(participantGate){participantGate.hidden=Boolean(flexParticipant&&flexParticipant.user_id)}if(flexParticipant&&flexParticipant.user_id)reconcileFoundation(flexParticipant);if(participantForm){participantForm.addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('participantStartStatus'),btn=document.getElementById('participantStartBtn'),name=document.getElementById('participantName').value.trim(),email=document.getElementById('participantEmail').value.trim();status.classList.remove('error');if(!email){status.textContent='Enter your email to start.';status.classList.add('error');return}btn.disabled=true;btn.textContent='STARTING…';try{const r=await fetch('/api/participants/start',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,lead_source:'website-foundation'})});const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Unable to start right now.');flexParticipant={user_id:d.participant.user_id,email:d.participant.email,name:d.participant.name||''};localStorage.setItem(FLEX_PARTICIPANT_KEY,JSON.stringify(flexParticipant));participantGate.hidden=true;await reconcileFoundation(flexParticipant)}catch(err){status.textContent=err&&err.message||'Unable to start right now.';status.classList.add('error');btn.disabled=false;btn.textContent='START THE 7-DAY FOUNDATION'}})}if(challengeRoot){challengeRoot.addEventListener('click',e=>{const btn=e.target.closest('[data-complete-day]');if(!btn||btn.disabled)return;setTimeout(async()=>{const p=readFlexParticipant(),local=readFoundationLocal();if(!p||!p.user_id||!local.completed.length)return;try{await postFoundationProgress(p.user_id,local.completed.length)}catch(err){console.warn('foundation_progress_sync_failed')}},0)})}\n`;

  source = source.replace('<main>', `<main>${gate}`);
  source = injectBefore(source, '</style>', css);
  source = injectBefore(source, '</script>', js);
  return source;
}

function tierClientScript({ tierId, totalDays, storageKey, serverUnlockTier, localUnlockScript }) {
  return `<script>(function(){const PKEY='${PARTICIPANT_KEY}',TIER='${tierId}',TOTAL=${totalDays},SKEY='${storageKey}';function participant(){try{return JSON.parse(localStorage.getItem(PKEY)||'null')}catch(e){return null}}function localCount(){try{const s=JSON.parse(localStorage.getItem(SKEY)||'{}');return Array.isArray(s.completed)?new Set(s.completed.filter(n=>Number.isInteger(n)&&n>=1&&n<=TOTAL)).size:0}catch(e){return 0}}async function sendProgress(){const p=participant(),day=localCount();if(!p||!p.user_id||!day)return;try{const r=await fetch('/api/participants/progress',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({user_id:p.user_id,tier_id:TIER,current_day:day})});const d=await r.json();if(r.ok&&d.ok&&d.progress&&d.progress.mastery_eligible)localStorage.setItem('flexStandard.mastery.eligible','true')}catch(e){console.warn('tier_progress_sync_failed')}}async function reconcile(){const p=participant();if(!p||!p.user_id)return;try{const r=await fetch('/api/participants/state?user_id='+encodeURIComponent(p.user_id),{headers:{'cache-control':'no-cache'}}),d=await r.json();if(!r.ok||!d.ok)return;const tiers=d.state.tiers||[],current=tiers.find(t=>t.tier_id===TIER),unlocked=tiers.some(t=>t.tier_id==='${serverUnlockTier}'&&(t.status==='unlocked'||t.status==='completed'));if(unlocked){${localUnlockScript}}if(current){const serverDay=Math.max(0,Math.min(TOTAL,Number(current.current_day||0))),lc=localCount();if(serverDay>lc){localStorage.setItem(SKEY,JSON.stringify({completed:Array.from({length:serverDay},(_,i)=>i+1)}));location.reload();return}else if(lc>serverDay){await sendProgress()}}}catch(e){console.warn('tier_state_reconcile_failed')}}document.addEventListener('click',e=>{const hit=e.target.closest('#completeBtn,#complete');if(!hit)return;setTimeout(sendProgress,0)});reconcile()})();</script>`;
}

export function enhanceMomentumParticipantFlow(source) {
  const script = tierClientScript({
    tierId: '14_day_momentum',
    totalDays: 14,
    storageKey: 'flexStandard.challenge14.v3',
    serverUnlockTier: '14_day_momentum',
    localUnlockScript: `if(localStorage.getItem('flexStandard.momentum14.unlocked')!=='true'){localStorage.setItem('flexStandard.momentum14.unlocked','true');location.reload();return}`,
  });
  return injectBefore(source, '</body>', script);
}

export function enhanceHabitLockParticipantFlow(source) {
  const script = tierClientScript({
    tierId: '21_day_habit_lock',
    totalDays: 21,
    storageKey: 'flexStandard.challenge21.v1',
    serverUnlockTier: '21_day_habit_lock',
    localUnlockScript: `try{const k='flexStandard.challenge14.v3',s=JSON.parse(localStorage.getItem(k)||'{}'),count=Array.isArray(s.completed)?new Set(s.completed).size:0;if(count<14){localStorage.setItem(k,JSON.stringify({completed:Array.from({length:14},(_,i)=>i+1)}));location.reload();return}}catch(e){localStorage.setItem('flexStandard.challenge14.v3',JSON.stringify({completed:Array.from({length:14},(_,i)=>i+1)}));location.reload();return}`,
  });
  return injectBefore(source, '</body>', script);
}
