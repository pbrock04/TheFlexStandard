# Validation trigger for challenge progression candidate; no deployment steps.
name: Mastery Feature CI

on:
  pull_request:
    paths:
      - 'src/mastery*.js'
      - 'test/mastery*.spec.js'
      - 'migrations/0003_mastery_v1.sql'
      - 'docs/MASTERY_V1_*.md'
      - '.github/workflows/mastery-ci.yml'
  workflow_dispatch:

jobs:
  verify-mastery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies
        run: npm ci

      - name: Validate Mastery Syntax
        run: node --check src/masteryLogic.js && node --check src/masteryApi.js

      - name: Run Test Suite
        run: npm test
